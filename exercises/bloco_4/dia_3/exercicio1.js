let valor = 5; //Declarei uma variavel para guardar o valor dado

for (let i = 0; i < valor; i += 1) {
  // criado uma repetição para repetir enquanto o indíce for menor que o valor da variavel, continuara repetindo até chegar no valor da variável
  console.log('*'.repeat(valor)); //criado um console para imprimir os astericos * o valor da variavel ex: se o valor for cinco imprimirá assim '*****' no console
}
